---
import site from "../data/site.json";

interface Props {
  placement?: string;
}

const { placement = "default" } = Astro.props;
const enabled = site.enableAds;
---

{
  enabled ? (
    <div class="ad-slot" data-slot={placement}></div>
  ) : (
    <div class="ad-slot" data-slot={placement} hidden></div>
  )
}
