---
import site from "../data/site.json";

interface Props {
  placement?: string;
}

const { placement = "default" } = Astro.props;
const enabled = import.meta.env.PUBLIC_ENABLE_ADS === "true" && site.enableAds;
---

{
  enabled ? (
    <div class="ad-slot" data-slot={placement}>
      <ins
        class="adsbygoogle"
        style="display:block"
        data-ad-client={site.adsensePublisher}
        data-ad-slot=""
        data-ad-format="auto"
        data-full-width-responsive="true"
      />
      <script is:inline>
        (adsbygoogle = window.adsbygoogle || []).push({});
      </script>
    </div>
  ) : (
    <div class="ad-slot" data-slot={placement} hidden></div>
  )
}
